## What changed?

## Checklist
- [ ] `npx eslint src/ai-tab-sorter.uc.js` passes
- [ ] `node scripts/validate-manifests.mjs` passes
- [ ] `node --test scripts/unit-tests.mjs` passes
- [ ] Tested on Zen: [stable / beta / alpha]
- [ ] Tested with ATG: [installed / not installed]
- [ ] Tested provider(s): [ollama / lmstudio / openai-compat / gemini / unreachable]
- [ ] No `removeTab()` in the sort path
- [ ] New prefs added to `PrefStore.DEFAULTS` (+ `preferences.json` if user-facing)
- [ ] `CHANGELOG.md` updated
